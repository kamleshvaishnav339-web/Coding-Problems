#include<iostream>
using namespace std;
int sumofdigit(int a){
    if(a==0)
    return 0;
    else
    return a%10 + sumdigit(a/10);
}
int main(){
    int n,sum=0;
    cout<<"Enter the number:";
    cin>>n;
    cout<<"SUm of digit of a number:"<<sumofdigit(n);
    return 0;
}
